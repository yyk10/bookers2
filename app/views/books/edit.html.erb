
<html>
<body>
  <div class="mx-auto" style="width: 200px;">

    <h2>Editing Book</h2>

      <%= form_with(model: @book, local: true) do |form| %>
      <% if @book.errors.any? %>
      <div id="error_explanation">
      <h3><%= pluralize(@book.errors.count, "error") %> prohibited book from being saved:</h3>
      <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
      </ul>
      </div>
      <% end %>

     <%= form_with model: @book do |f| %>
      <label for="inputName">Title</label>
      <div><%= f.text_field :title, autofocus: true, id:"BookName", placeholder:""%>
      <div><label for="inputIntroduction">Opinion</label>
      <div><%= f.text_area :body, autofocus: true, id:"caption",placeholder:""%>
      <div><%= f.submit "Update Book",class: "btn btn-success" %>
     <% end %>
     <%= link_to "Show" , book_path(@book) %>
     |
    <%= link_to "Back" , books_path(@book) %>
    </div>
    </body>
    </html>